<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Spinner para seleccionar la categoría de mascota -->
    <Spinner
        android:id="@+id/category"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginTop="40dp"
        android:layout_gravity="center"
        android:layout_margin="16dp"
        android:entries="@array/pet_categories" />

    <!-- Botón para buscar las razas -->
    <Button
        android:id="@+id/find_pet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="16dp"
        android:text="@string/button_find_pet"
        android:onClick="onClickFindPet" />

    <!-- TextView para mostrar las razas -->
    <TextView
        android:id="@+id/breeds"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="16dp"
        android:text="@string/text_no_breed" />
</LinearLayout>